# patterns/

再利用パターン候補を置く（未ルール）。

- `OBSERVED` / `REPEATED` の観測は原則 `../observations/`
- Agent 行動を変える `PROMOTED` / 正本参照の `LOCKED_REFERENCE` は `../rules/`
- 本ディレクトリのエントリは行動規則ではない。`promotion-policy.md` に従い昇格する

v1 初期 seed では patterns 本文は 0 件（catalog に未掲載）。
