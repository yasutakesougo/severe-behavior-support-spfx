# 合成fixtureの規則

このディレクトリには、実在情報を含まないテストデータだけを置きます。

実運用データを加工した値は使用しません。

利用者名、職員名、実在ID、実在コード、事業所名、原資料名は使用しません。

識別子と説明文には、`synthetic-*`などの合成値を使用します。

日付には、対象スキーマに適合するテスト専用の固定日を使用します。

日付形式の検証では、ISO 8601など契約で定めた形式を使用します。

fixtureごとに、入力の目的と期待結果をテスト内へ記録します。

欠損、不正値、非該当、不適格、算定不能は、異なる期待結果として扱います。
