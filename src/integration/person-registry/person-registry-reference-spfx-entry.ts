/**
 * Type-only SPFx bridge entry for the canonical person-registry reference.
 *
 * Runtime person-registry values and authority providers stay outside SPFx.
 */
export type { LegacyPersonMappingKey, ReverseLegacyResolution } from "./person-registry-reference";
