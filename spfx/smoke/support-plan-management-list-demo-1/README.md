# SUPPORT-PLAN-MANAGEMENT-LIST-DEMO-1 browser smoke

Synthetic Planning PC support-plan management list (`presentationRole=PLANNER`).

```bash
cd spfx
node smoke/support-plan-management-list-demo-1/run-smoke.mjs
```

Requires esbuild / puppeteer-core / sass under `/tmp/node_modules` (or `SPML_*_PATH` overrides).

NO SharePoint / Deploy / production write / Schema change / LIVE WRITE.
