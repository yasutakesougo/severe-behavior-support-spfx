# FIELD-WORKFLOW UI (#356) browser smoke

Synthetic-only ~390px field flow evidence.

```bash
cd spfx
node smoke/field-workflow-ui/run-smoke.mjs
```

Requires esbuild / puppeteer-core / sass under `/tmp/node_modules` (or `FW_*_PATH` overrides).

NO SharePoint / Deploy / production write.
