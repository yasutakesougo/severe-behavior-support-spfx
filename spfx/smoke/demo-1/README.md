# DEMO-1 browser smoke

Synthetic FIELD_STAFF entry evidence for `DEMO_1_FIELD_STAFF_FIXTURE`
(same entry as `ScaffoldShellWebPart`).

```bash
cd spfx
node smoke/demo-1/run-smoke.mjs
```

Requires esbuild / puppeteer-core / sass under `/tmp/node_modules`
(or `DEMO1_*_PATH` overrides).

Artifacts: `/opt/cursor/artifacts/demo-1-browser-smoke/`

NO SharePoint / Deploy / production write.
