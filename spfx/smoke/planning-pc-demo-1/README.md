# PLANNING-PC-DEMO-1 browser smoke

Synthetic Planning PC support-plan graph (`presentationRole=PLANNER`).

```bash
cd spfx
node smoke/planning-pc-demo-1/run-smoke.mjs
```

Requires esbuild / puppeteer-core / sass under `/tmp/node_modules` (or `PPC_*_PATH` overrides).

NO SharePoint / Deploy / production write / Schema change.
